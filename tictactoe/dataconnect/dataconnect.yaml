specVersion: "v1"
serviceId: "tictactoe"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "tictactoe-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
